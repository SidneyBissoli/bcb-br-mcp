; agents lista @modelcontextprotocol/* como peers rígidos; sem esta flag o npm
; instalaria uma SEGUNDA cópia do SDK em worker/node_modules, quebrando o desenho de
; cópia única (o worker resolve o SDK do node_modules do pacote pai).
legacy-peer-deps=true
